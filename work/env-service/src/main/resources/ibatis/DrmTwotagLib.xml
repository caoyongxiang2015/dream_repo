<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" 
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="com.env.dto.DrmTwotagLib">

	<typeAlias alias="drmTwotagLibEntity" type="com.env.dto.DrmTwotagLib" />
	<resultMap id="drmTwotagLib" class="com.env.dto.DrmTwotagLib">
		<!-- jdbcType="int" javaType="java.lang.Integer" -->
		<!-- jdbcType="varchar" javaType="java.lang.String" -->
		<!-- jdbcType="datetime" javaType="java.util.Date" -->
		<result property="id" column="id" />
		<result property="oneTagId" column="one_tag_id" />
		<result property="tagName" column="tag_name" />
		<result property="version" column="version" />
		<result property="disabled" column="disabled" />
		<result property="createUserid" column="create_userid" />
		<result property="createTime" column="create_time" />
		<result property="updateUserid" column="update_userid" />
		<result property="updateTime" column="update_time" />
		
	</resultMap>

	<!-- 用于select查询公用抽取的列 -->
	<sql id="drmTwotagLib.columns">
	    <![CDATA[
        id, one_tag_id, tag_name, version, disabled, create_userid, create_time, update_userid, update_time
	    ]]>
	</sql>

	<!-- 新增记录 -->
	<insert id="save" parameterClass="drmTwotagLibEntity">
	    <![CDATA[
	        INSERT INTO DRM_TWOTAG_LIB (
	        	id, 
	        	one_tag_id, 
	        	tag_name, 
	        	version, 
	        	disabled, 
	        	create_userid, 
	        	create_time, 
	        	update_userid, 
	        	update_time
	        ) VALUES (
	        	#id#, 
	        	#oneTagId#, 
	        	#tagName#, 
	        	#version#, 
	        	#disabled#, 
	        	#createUserid#, 
	        	#createTime#, 
	        	#updateUserid#, 
	        	#updateTime#
	        )
	    ]]>
		<selectKey resultClass="java.lang.Integer" type="post"
			keyProperty="id">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>

	<!-- 新增记录 -->
	<insert id="insert" parameterClass="drmTwotagLibEntity">
	    <![CDATA[
	        INSERT INTO DRM_TWOTAG_LIB (
	        	id, 
	        	one_tag_id, 
	        	tag_name, 
	        	version, 
	        	disabled, 
	        	create_userid, 
	        	create_time, 
	        	update_userid, 
	        	update_time
	        ) VALUES (
	        	#id#, 
	        	#oneTagId#, 
	        	#tagName#, 
	        	#version#, 
	        	#disabled#, 
	        	#createUserid#, 
	        	#createTime#, 
	        	#updateUserid#, 
	        	#updateTime#
	        )
	    ]]>
		<selectKey resultClass="java.lang.Integer" type="post"
			keyProperty="">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>

	<!-- 通过ID修改 -->
	<update id="update" parameterClass="drmTwotagLibEntity">
	    <![CDATA[
	        UPDATE DRM_TWOTAG_LIB SET
		        id = #id#, 
		        one_tag_id = #oneTagId#, 
		        tag_name = #tagName#, 
		        version = #version#, 
		        disabled = #disabled#, 
		        create_userid = #createUserid#, 
		        create_time = #createTime#, 
		        update_userid = #updateUserid#, 
		        update_time = #updateTime#
	        WHERE 
	    ]]>
	</update>

	<!-- 通过ID删除 -->
	<delete id="delete">
	    <![CDATA[
	        DELETE FROM DRM_TWOTAG_LIB WHERE
	    ]]>
	</delete>

	<!-- 通过ID列表批量删除 -->
	<delete id="deleteByIds" parameterClass="java.lang.String">
	    <![CDATA[
	        DELETE FROM DRM_TWOTAG_LIB WHERE
	    ]]>
	</delete>

	<!-- 通过ID查询 -->
	<select id="getById" resultMap="drmTwotagLib">
		SELECT
		<include refid="drmTwotagLib.columns" />
	    <![CDATA[
	        FROM DRM_TWOTAG_LIB 
	        WHERE 
	    ]]>
	</select>

	<!-- 用于分页查询抽取的公共的条件 -->
	<sql id="drmTwotagLib.queryPage">
		<dynamic prepend="WHERE">
		    <isNotNull property="entity">
				<isNotEmpty prepend="AND" property="entity.id">
		            <![CDATA[
					id = #entity.id#
		            ]]>
				</isNotEmpty>
				<isNotEmpty prepend="AND" property="entity.oneTagId">
		            <![CDATA[
					one_tag_id = #entity.oneTagId#
		            ]]>
				</isNotEmpty>
				<isNotEmpty prepend="AND" property="entity.tagName">
		            <![CDATA[
					tag_name = #entity.tagName#
		            ]]>
				</isNotEmpty>
				<isNotEmpty prepend="AND" property="entity.version">
		            <![CDATA[
					version = #entity.version#
		            ]]>
				</isNotEmpty>
				<isNotEmpty prepend="AND" property="entity.disabled">
		            <![CDATA[
					disabled = #entity.disabled#
		            ]]>
				</isNotEmpty>
				<isNotEmpty prepend="AND" property="entity.createUserid">
		            <![CDATA[
					create_userid = #entity.createUserid#
		            ]]>
				</isNotEmpty>
				<isNotEmpty prepend="AND" property="entity.createTime">
		            <![CDATA[
					create_time = #entity.createTime#
		            ]]>
				</isNotEmpty>
				<isNotEmpty prepend="AND" property="entity.updateUserid">
		            <![CDATA[
					update_userid = #entity.updateUserid#
		            ]]>
				</isNotEmpty>
				<isNotEmpty prepend="AND" property="entity.updateTime">
		            <![CDATA[
					update_time = #entity.updateTime#
		            ]]>
				</isNotEmpty>
			</isNotNull>
		</dynamic>
	</sql>

	<!-- 为分页查询符合条件的记录数 -->
	<select id="queryByPage_count" resultClass="int">
		SELECT count(*) FROM DRM_TWOTAG_LIB
		<include refid="drmTwotagLib.queryPage" />
	</select>

	<!-- 分页查询指定页记录 -->
	<select id="queryByPage" resultMap="drmTwotagLib">
		SELECT
		<include refid="drmTwotagLib.columns" />
	    <![CDATA[
	        FROM DRM_TWOTAG_LIB 
	    ]]>
		<include refid="drmTwotagLib.queryPage" />
		<![CDATA[
		    LIMIT #paging.startRecord#, #paging.pageSize#
        ]]>
	</select>

	<!-- 查询所有记录 -->
	<select id="queryAll" resultMap="drmTwotagLib">
		SELECT
		<include refid="drmTwotagLib.columns" />
	    <![CDATA[
		    FROM DRM_TWOTAG_LIB
	    ]]>
	</select>
</sqlMap>