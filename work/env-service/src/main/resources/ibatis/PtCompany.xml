<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" 
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="com.env.dto.PtCompany">

	<typeAlias alias="ptCompanyEntity" type="com.env.dto.PtCompany" />
		
	<resultMap id="ptCompany" class="com.env.dto.PtCompany">
		<result property="id" column="id" />
		<result property="companyName" column="company_name" />
		
		<result property="version" column="version" jdbcType="int" javaType="java.lang.Integer"/>
		<result property="disabled" column="disabled" jdbcType="int" javaType="java.lang.Integer"/>
		<result property="createUserid" column="create_userid" jdbcType="int" javaType="java.lang.Integer"/>
		<result property="createTime" column="create_time" jdbcType="datetime" javaType="java.util.Date"/>
		<result property="updateUserid" column="update_userid" jdbcType="int" javaType="java.lang.Integer"/>
		<result property="updateTime" column="update_time" jdbcType="datetime" javaType="java.util.Date"/>
	</resultMap>


	<!-- 用于select查询公用抽取的列 -->
	<sql id="ptCompany.columns">
	    <![CDATA[
        id, company_name, version, disabled, create_userid, create_time, update_userid, update_time
	    ]]>
	</sql>

	<!-- 新增记录 -->
	<insert id="save" parameterClass="ptCompanyEntity">
	    <![CDATA[
	        INSERT INTO PT_COMPANY (
	        	id, 
	        	company_name, 
	        	version, 
	        	disabled, 
	        	create_userid, 
	        	create_time, 
	        	update_userid, 
	        	update_time
	        ) VALUES (
	        	#id#, 
	        	#companyName#, 
	        	#version#, 
	        	#disabled#, 
	        	#createUserid#, 
	        	#createTime#, 
	        	#updateUserid#, 
	        	#updateTime#
	        )
	    ]]>
		<selectKey resultClass="java.lang.Integer" type="post"
			keyProperty="id">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>

	<!-- 新增记录 -->
	<insert id="insert" parameterClass="ptCompanyEntity">
	    <![CDATA[
	        INSERT INTO PT_COMPANY (
	        	id, 
	        	company_name, 
	        	version, 
	        	disabled, 
	        	create_userid, 
	        	create_time, 
	        	update_userid, 
	        	update_time
	        ) VALUES (
	        	#id#, 
	        	#companyName#, 
	        	#version#, 
	        	#disabled#, 
	        	#createUserid#, 
	        	#createTime#, 
	        	#updateUserid#, 
	        	#updateTime#
	        )
	    ]]>
		<selectKey resultClass="java.lang.Integer" type="post"
			keyProperty="id">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>

	<!-- 通过ID修改 -->
	<update id="update" parameterClass="ptCompanyEntity">
	    <![CDATA[
	        UPDATE PT_COMPANY SET
		        company_name = #companyName#, 
		        version = #version#, 
		        disabled = #disabled#, 
		        create_userid = #createUserid#, 
		        create_time = #createTime#, 
		        update_userid = #updateUserid#, 
		        update_time = #updateTime#
	        WHERE 
		        id = #id#
	    ]]>
	</update>

	<!-- 通过ID删除 -->
	<delete id="delete">
	    <![CDATA[
	        DELETE FROM PT_COMPANY WHERE
	        id = #id#
	    ]]>
	</delete>

	<!-- 通过ID列表批量删除 -->
	<delete id="deleteByIds" parameterClass="java.lang.String">
	    <![CDATA[
	        DELETE FROM PT_COMPANY WHERE
	        id IN ($ids$)
	    ]]>
	</delete>

	<!-- 通过ID查询 -->
	<select id="getById" resultMap="ptCompany">
		SELECT
		<include refid="ptCompany.columns" />
	    <![CDATA[
	        FROM PT_COMPANY 
	        WHERE 
		        id = #id#
	    ]]>
	</select>

	<!-- 用于分页查询抽取的公共的条件 -->
	<sql id="ptCompany.queryPage">
		<dynamic prepend="WHERE">
		    <isNotNull property="entity">
				<isNotEmpty prepend="AND" property="entity.id">
		            <![CDATA[
					id = #entity.id#
		            ]]>
				</isNotEmpty>
				<isNotEmpty prepend="AND" property="entity.companyName">
		            <![CDATA[
					company_name = #entity.companyName#
		            ]]>
				</isNotEmpty>
				<isNotEmpty prepend="AND" property="entity.version">
		            <![CDATA[
					version = #entity.version#
		            ]]>
				</isNotEmpty>
				<isNotEmpty prepend="AND" property="entity.disabled">
		            <![CDATA[
					disabled = #entity.disabled#
		            ]]>
				</isNotEmpty>
				<isNotEmpty prepend="AND" property="entity.createUserid">
		            <![CDATA[
					create_userid = #entity.createUserid#
		            ]]>
				</isNotEmpty>
				<isNotEmpty prepend="AND" property="entity.createTime">
		            <![CDATA[
					create_time = #entity.createTime#
		            ]]>
				</isNotEmpty>
				<isNotEmpty prepend="AND" property="entity.updateUserid">
		            <![CDATA[
					update_userid = #entity.updateUserid#
		            ]]>
				</isNotEmpty>
				<isNotEmpty prepend="AND" property="entity.updateTime">
		            <![CDATA[
					update_time = #entity.updateTime#
		            ]]>
				</isNotEmpty>
			</isNotNull>
		</dynamic>
	</sql>

	<!-- 为分页查询符合条件的记录数 -->
	<select id="queryByPage_count" resultClass="int">
		SELECT count(*) FROM PT_COMPANY
		<include refid="ptCompany.queryPage" />
	</select>

	<!-- 分页查询指定页记录 -->
	<select id="queryByPage" resultMap="ptCompany">
		SELECT
		<include refid="ptCompany.columns" />
	    <![CDATA[
	        FROM PT_COMPANY 
	    ]]>
		<include refid="ptCompany.queryPage" />
		<![CDATA[
		    LIMIT #paging.startRecord#, #paging.pageSize#
        ]]>
	</select>

	<!-- 查询所有记录 -->
	<select id="queryAll" resultMap="ptCompany">
		SELECT
		<include refid="ptCompany.columns" />
	    <![CDATA[
		    FROM PT_COMPANY
	    ]]>
	</select>

</sqlMap>